import React, { Component } from 'react';

export default class SendMessage extends Component {

    render() {
        return (
            <div>
                <form action="">
                    <input type="text"/>
                    <button>SEND</button>
                </form>
            </div>
        );
    }
}
